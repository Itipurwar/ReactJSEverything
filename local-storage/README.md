# Local Storage Demo in React

This React app demonstrates the use of **Local Storage** to store, retrieve, and remove data.

## Features
- Store simple values and objects in Local Storage
- Retrieve stored data using `JSON.parse()`
- Remove individual items or clear all storage
- Works with both `localStorage` and `sessionStorage`

